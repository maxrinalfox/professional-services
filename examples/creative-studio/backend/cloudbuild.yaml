# Copyright 2025 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

steps:
  # Step 0: Validate required secrets are configured (and not placeholders)
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'Validate Backend Secrets'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        #!/bin/bash
        set -e

        echo "════════════════════════════════════════════════════════════════"
        echo "🔐 VALIDATING REQUIRED BACKEND SECRETS"
        echo "════════════════════════════════════════════════════════════════"
        echo ""

        # Backend secrets that must be manually configured
        declare -A REQUIRED_SECRETS=(
          [GOOGLE_TOKEN_AUDIENCE]="OAuth 2.0 Client ID for JWT token audience validation"
        )

        FAILED_SECRETS=()
        PLACEHOLDER_PATTERN="placeholder_.*_will_be_updated"

        for SECRET_NAME in "${!REQUIRED_SECRETS[@]}"; do
          SECRET_VALUE="${!SECRET_NAME}"
          SECRET_DESC="${REQUIRED_SECRETS[$SECRET_NAME]}"

          # Check if secret exists and is not empty
          if [ -z "$SECRET_VALUE" ]; then
            echo "❌ MISSING: $SECRET_NAME"
            echo "   Description: $SECRET_DESC"
            echo "   Status: [EMPTY]"
            FAILED_SECRETS+=("$SECRET_NAME")
            echo ""
          elif [[ "$SECRET_VALUE" =~ $PLACEHOLDER_PATTERN ]]; then
            echo "❌ NOT POPULATED: $SECRET_NAME"
            echo "   Description: $SECRET_DESC"
            echo "   Status: [PLACEHOLDER - needs real value]"
            FAILED_SECRETS+=("$SECRET_NAME")
            echo ""
          else
            echo "✅ VALID: $SECRET_NAME"
            echo "   Description: $SECRET_DESC"
            echo ""
          fi
        done

        echo "════════════════════════════════════════════════════════════════"

        if [ ${#FAILED_SECRETS[@]} -gt 0 ]; then
          echo ""
          echo "❌ BUILD FAILED: ${#FAILED_SECRETS[@]} secret(s) not properly configured"
          echo ""
          echo "Missing or placeholder secrets:"
          for SECRET in "${FAILED_SECRETS[@]}"; do
            echo "  • $SECRET"
          done
          echo ""
          echo "HOW TO FIX:"
          echo "─────────────────────────────────────────────────────────────"
          echo "The GOOGLE_TOKEN_AUDIENCE secret must be set to your OAuth 2.0"
          echo "Client ID (OAUTH_CLIENT_ID in Secret Manager)."
          echo ""
          echo "This is the SAME value as used by the frontend."
          echo ""
          echo "Steps:"
          echo "1. If you haven't created the OAuth Client ID in GCP Console:"
          echo "   https://console.cloud.google.com/apis/credentials?project=${PROJECT_ID}"
          echo ""
          echo "2. Once you have the Client ID, populate the secret:"
          echo "   $ gcloud secrets versions add OAUTH_CLIENT_ID \\"
          echo "       --data-file=- --project=${PROJECT_ID} \\"
          echo "       <<< \"YOUR_CLIENT_ID.apps.googleusercontent.com\""
          echo ""
          echo "3. Cloud Run will automatically mount it as GOOGLE_TOKEN_AUDIENCE"
          echo ""
          echo "4. After populating the secret, retry the backend build."
          echo "─────────────────────────────────────────────────────────────"
          exit 1
        fi

        echo "✅ All required backend secrets are properly configured!"
        echo ""
        echo "Note: Database password is auto-configured via Terraform."
        echo ""

    secretEnv:
      - GOOGLE_TOKEN_AUDIENCE

  # Step 1: Build the container image using the standard Docker builder.
  # Using SHORT_SHA for the tag is a best practice for traceability.
  # Builds the backend service using Dockerfile.backend
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build'
    env: ['DOCKER_BUILDKIT=1']
    args:
      [
        'build',
        '-t',
        '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO_NAME}/${_SERVICE_NAME}:$SHORT_SHA',
        '-f',
        'Dockerfile.backend',
        '.',
      ]
    # This ensures the build context is the 'backend' folder
    dir: 'examples/creative-studio/backend'

  # Step 2: Push the container image to Google Artifact Registry.
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push'
    args:
      [
        'push',
        '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO_NAME}/${_SERVICE_NAME}:$SHORT_SHA',
      ]

  # Step 3: Deploy to Cloud Run using the gcloud CLI builder.
  # This command creates or updates the service with:
  # - New container image
  # - Environment variables (from Terraform via _BACKEND_ENV_VARS)
  # - Runtime secrets mapped from Secret Manager (from Terraform via _BACKEND_SECRETS)
  #
  # Why set env vars and secrets in Cloud Build (not Terraform)?
  # - Terraform creates the service infrastructure without env vars/secrets
  # - This allows the service to exist while IAM bindings are being created
  # - Cloud Build sets env vars/secrets AFTER the service exists and IAM bindings are ready
  # - Terraform's lifecycle.ignore_changes=[...env] prevents it from reverting these changes
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO_NAME}/${_SERVICE_NAME}:$SHORT_SHA'
      - '--region'
      - '${_REGION}'
      - '--set-env-vars=${_BACKEND_ENV_VARS}'
      - '--set-secrets=${_BACKEND_SECRETS}'
      - '--quiet' # Suppress interactive prompts for CI/CD environments

# BEST PRACTICE: Explicitly declare the image(s) built by this pipeline.
# This integrates the build with Google Cloud's security features, like showing
# vulnerability scan results in the Cloud Build UI.
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO_NAME}/${_SERVICE_NAME}:$SHORT_SHA'

# Substitutions will be passed in from the Cloud Build trigger.
# You can override these for manual runs.
substitutions:
  _SERVICE_NAME: 'creative-studio-backend'
  _REPO_NAME: 'cs-be-development-repo' # This matches local.artifact_repo_id
  _REGION: 'us-central1' # TODO: Make Region generic from users input

  # Environment variables (comma-separated KEY=VALUE pairs)
  # Set by Terraform Cloud Build trigger from backend module
  _BACKEND_ENV_VARS: 'LOG_LEVEL=INFO,ENVIRONMENT=development'

  # Runtime secrets (comma-separated ENV_VAR=SECRET_NAME:VERSION pairs)
  # Set by Terraform Cloud Build trigger from backend module
  # Maps GOOGLE_TOKEN_AUDIENCE env var to OAUTH_CLIENT_ID secret
  _BACKEND_SECRETS: 'GOOGLE_TOKEN_AUDIENCE=OAUTH_CLIENT_ID:latest'

options:
  logging: CLOUD_LOGGING_ONLY
